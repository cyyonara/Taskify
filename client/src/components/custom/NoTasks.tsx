const NoTasks: React.FC = () => {
  return (
    <main className="flex-1 flex h-full">
      <h3 className="m-auto">You don't have any task at the moment</h3>
    </main>
  );
};

export default NoTasks;
